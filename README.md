# Secure-Multiprocessor-TCP-Application-Server
This report documents the design and implementation of a Secure Multiprocessor TCP Application Server for the IE2102 Network Programming module. The server is implemented in C using TCP sockets and multiprocessing (fork()), while the client is implemented in Python.
